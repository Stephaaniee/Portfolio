#include <stdio.h>
#include <math.h>

int main()
{
    int num;


    /*printf("Enter value 1 to 100\t");
    scanf("%d", &num);

    if (num < 1 || num > 100)
    puts("Wrong number !!!");

    else
    printf("%d", num);

    for (num = 1; num <= 100; num++)
    {
        printf("%d\n", num);
    }
    return 0;

    //tas2
    for (num = 1; num <= 100; num++)
    {
        printf("%d\t", num);
    }
    return 0;


    //task 3
    for (num = 100; num >= 1; num--)
    {
        printf("%d\n", num);
    }
    return 0;*/

    //task 4
    num = 1;
    int i;
    i = 0;
    while (i < 20)
    {
        ("%d\n", pow(2, num));
        num = num * 2;
        i++;
    }
}